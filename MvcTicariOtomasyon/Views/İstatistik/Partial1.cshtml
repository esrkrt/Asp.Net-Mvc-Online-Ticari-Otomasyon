@model IEnumerable<MvcTicariOtomasyon.Models.Siniflar.SinifGrup2>
<div class="card">
    <div class="card-header">
        <h3 class="card-title">Departman Personel Bilgileri</h3>
    </div>
    <!-- /.card-header -->
    <div class="card-body p-0">
        <table class="table table-sm">
            <thead>
                <tr>
                    <th style="width: 10px">#</th>
                    <th>Departman</th>
                    <th>Progress Bar</th>
                    <th style="width: 40px;">Sayı</th>
                </tr>
            </thead>
            <tbody>
                @{ int value = 0;}
                @foreach (var x in Model)
                {

                    <tr>
                        <td>@{value++;} @value</td>
                        <td>@x.Departman</td>
                        <td>
                            @if (x.Sayi == 1)
                            {
                                <div class="progress progress-xs">
                                    <div class="progress-bar progress-bar-success" style="width: @x.Sayi%"></div>
                                </div>
                            }
                            else if (x.Sayi == 2)
                            {
                                <div class="progress progress-xs">
                                    <div class="progress-bar bg-warning" style="width: @x.Sayi%"></div>
                                </div>
                            }
                        </td>
                        @if (x.Sayi == 1)
                        {
                            <td><span class="badge bg-success">@x.Sayi</span></td>

                        }
                        else if (x.Sayi == 2)
                        {
                            <td><span class="badge bg-warning">@x.Sayi</span></td>

                        }
                    </tr>
                }

            </tbody>
        </table>
    </div>
    <!-- /.card-body -->
</div>

